; HeadTracking configuration. Place next to FalloutNV.exe.
; Configure sensitivity, deadzones and centring in your tracker.

[Network]
; UDP port for OpenTrack data (default: 4242)
; Must match OpenTrack output configuration. Restart the game after changing it.
Port=4242

[Smoothing]
; Which value is used depends on where the tracking arrives from, so a local
; tracker and a phone on WiFi can each have their own amount.
; Smoothing applied when the tracker runs on this machine (loopback).
; 0 = no smoothing, 1 = heavy. Covers rotation and position.
LocalSmoothing=0.0
; Smoothing applied when the tracker is a remote device on the network.
; 0 = no smoothing, 1 = heavy. Covers rotation and position.
RemoteSmoothing=0.15

[Hotkeys]
; Nav-cluster virtual key codes (hex). Each action also accepts a fixed
; Ctrl+Shift+<letter> chord (Y/G/H/J) which is not configurable.
; End=0x23, PageUp=0x21, PageDown=0x22
;
; Avoid keys used by the game:
;   F5=Quicksave, F9=Quickload, TAB=Pip-Boy, ESC=Pause
Toggle=0x23
CycleTrackingMode=0x21
ReticleToggle=0x22
; Delete / Ctrl+Shift+J toggles yaw mode
YawModeKey=0x2E
DebounceMs=200

[GameState]
; Input blocking mode - when to disable hotkeys
; 0 = Never (always process hotkeys, even in menus)
; 1 = Menus only (block during pause menu, pipboy, etc.)
; 2 = All dialogue (block during menus AND NPC conversations)
; 3 = All overlays (block during any overlay: menus, console, VATS, etc.)
; Default: 0 (hotkeys always work)
InputBlockMode=0

; Track in third-person camera mode (1=yes, 0=no)
; Default: 1 (head tracking works in both first and third person)
TrackInThirdPerson=1

; Track during VATS (1=yes, 0=no)
; Default: 0 (VATS has its own camera control)
TrackInVATS=0

; Pause tracking during combat (1=yes, 0=no)
; Default: 0 (head tracking continues during combat)
PauseDuringCombat=0

[Feedback]
; Reserved setting; this build does not emit game console messages.
ShowMessages=1

[Camera]
; 1 = horizon-locked yaw, 0 = camera-local yaw
WorldSpaceYaw=1
